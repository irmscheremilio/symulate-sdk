# Local Testing Configuration (optional)
# Copy this file to .env.local and update with your local test credentials

# Use this for testing the SDK locally against a test/local Supabase instance
# These values are used by build:local to create a test build

SYMULATE_PLATFORM_URL=http://localhost:3000
SYMULATE_SUPABASE_URL=http://localhost:54321
SYMULATE_SUPABASE_ANON_KEY=your-local-test-anon-key
